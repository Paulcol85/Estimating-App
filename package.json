{
  "name": "cc-sales-intake",
  "version": "1.0.0",
  "description": "Coleridge Construction — Sales Intake App",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "dev": "concurrently \"node server.js\" \"npm run dev --prefix client\"",
    "build": "npm install --prefix client && npm run build --prefix client",
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "node-fetch": "^3.3.2",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
